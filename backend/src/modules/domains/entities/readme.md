# Why the "entities" directory?
~~~~
- It determines the logical structure that represents a database entity and the way data can be manipulated and organized, such as hashing the user's password before saving (Hook "beforeSave" in ORM Sequelize).